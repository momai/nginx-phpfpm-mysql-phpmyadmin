name: 'test my project'
on:
  push:
jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v2
    
    - name: 'test'
      run: echo secrets.KEY >> authorized_keys

    - name: 'test2'
      run: ssh secrets.USENAME@34.123.108.90 'uptime'
